language: node_js
sudo: enabled
node_js:
  - 7
services:
  - mysql
before_install:
  - mysql -e '\./db/repo.txt; \./db/repo_queue.txt'
install:
  - npm install
script:
- echo "skipping tests"
- node repo.js
